export const formatTweetOutput = (formData) => ({
  "project": "🧑 DFS Project",
  "current node": formData.currentNode,
  "parent node": formData.parentNode,
  "fun fact": formData.funFact,
  "nodes visited": formData.nodesVisited
});

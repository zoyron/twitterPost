export const createTweetContent = ({ currentNode, parentNode, funFact, nodesVisited }) => {
  return {
    project: "🧑 DFS Project",
    "current node": currentNode,
    "parent node": parentNode,
    "fun fact": funFact,
    "nodes visited": nodesVisited
  };
};